[package]
name = "utils"
version = "0.1.0"
edition = "2021"

# It is MANDATORY for the project to generate a dynamic library.
[lib]
crate-type = ["cdylib"]

[dependencies]
reqwest = { version = "0.12.5", features = ["blocking"] }
